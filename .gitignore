build/
build-*/
.cache/
.DS_Store
*.o
*.a
compile_commands.json

# Build + packaging outputs
artifacts/
build/

# Web demo build + generated site output (never commit .wasm / generated JS).
web/build/
docs/
_site/

# playwright-core is installed on demand for the headless OG render (CI + local).
node_modules/
